#!/usr/bin/env bash
# Bundle wrapper for mise Ruby environments

cd "$(dirname "$0")/.." || exit 1

# Use bundler directly
BUNDLER_PATH=$(mise exec ruby -- ruby -e "puts Gem::Specification.find_by_name('bundler').full_gem_path + '/exe/bundle'")
mise exec ruby -- ruby "$BUNDLER_PATH" "$@"